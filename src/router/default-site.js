// import {gbs} from 'config'

// export default function (to, from, next) {
//   if (to.path === '/') {
//     let type = this.$store.getters.userinfo ? 'private' : 'public'
//     next(`/${gbs.default_site}/${type}`)
//   } else {
//     next()
//   }
// }
